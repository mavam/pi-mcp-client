pre-push:
  parallel: true
  commands:
    check:
      run: bun run check
    build:
      run: bun run build
    changelog-validate:
      glob: "changelog/**"
      run: uvx tenzir-ship validate --lenient
    actionlint:
      run: if command -v actionlint >/dev/null; then actionlint; fi
